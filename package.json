{
  "name": "restaurants-dashboard",
  "version": "1.0.0",
  "description": "Simple restaurant menus overview app.\r Built with Tauri, so there is support for browser, desktop and mobile.",
  "scripts": {
    "dev:frontend": "cd app/tauri-app && npm install && npm run dev",
    "dev:backend": "cd backend && npm install && npm run dev",
    "dev:proxy": "cd proxy && npm install && npm run dev",
    "dev": "npm-run-all --parallel dev:*",
    "deploy:frontend": "cd app/tauri-app && npm run deploy",
    "deploy:backend": "cd backend && npm run deploy",
    "deploy:proxy": "cd proxy && npm run deploy",
    "deploy": "npm-run-all --parallel deploy:*"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/xxxmoney/restaurants-dashboard.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/xxxmoney/restaurants-dashboard/issues"
  },
  "homepage": "https://github.com/xxxmoney/restaurants-dashboard#readme",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
