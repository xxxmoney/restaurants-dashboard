# Frontend - Tauri App

## Prerequisites

Install packages:
`npm install`

## Running the app

To run the app, simply run this:
`npm run dev`

## Publish to Github Pages

To publish, simply run this:
`npm run deploy`

